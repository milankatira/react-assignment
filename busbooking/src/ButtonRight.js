import React from 'react'

const ButtonRight = (props) => {
    return (
        <div className="buttonRight">
            <button>
            {props.number}
            </button>
        </div>
    )
}

export default ButtonRight
